import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

/*
  This is the main entry point of the app. It renders the App component.
*/

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
